import React from 'react';
import {ActivityIndicator} from 'react-native';

const Loader = () => {
    return <ActivityIndicator color="baby-blue" size='large' style={{flex: 1, justifyContent: 'center' }}/>
};


export default Loader;